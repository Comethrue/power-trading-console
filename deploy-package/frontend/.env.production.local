# ============================================
# 电力交易AI平台 - 本地部署配置
# ============================================
# 此文件用于本地部署时的配置

# 前端基础路径（本地部署使用根路径）
VITE_BASE_URL=/

# 后端API地址（本地部署使用本地服务，注意前端在/console路径）
VITE_API_BASE=http://127.0.0.1:8000
